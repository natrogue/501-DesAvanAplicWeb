// src/login/login-entry.tsx
import React from 'react';
import ReactDOM from 'react-dom/client';
//import Login from './login.tsx';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>

  </React.StrictMode>
);